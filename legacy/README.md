# Legacy PowerShell version

The original PowerShell prototype of storm. Frozen — receives bugfixes
only, no new features. See the root `README.md` for the Node version.

## Usage

Copy `storm.ps1` and `storm.cmd` to the same folder and add that folder
to your PATH. Running `storm` from any CMD/PowerShell will launch it.
